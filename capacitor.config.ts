import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.vinmarts.com',
  appName: 'VinMart',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
